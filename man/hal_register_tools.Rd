% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-builder.R
\name{hal_register_tools}
\alias{hal_register_tools}
\title{Register Multiple Tools on the Active Session}
\usage{
hal_register_tools(tools)
}
\arguments{
\item{tools}{A list of tool definitions (\code{ellmer::ToolDef} objects or hal
tool lists).}
}
\value{
Invisibly, the number of tools registered.
}
\description{
Registers a list of tools on the active \code{hal()} session so the model can
call them. Each element may be an \code{ellmer::ToolDef} (e.g. from
\code{daisy::daisy_tools()}) or a plain hal tool list with \code{name} / \code{description}
/ \code{fun} / \code{parameters}. Unlike \code{\link[=hal_register_tool]{hal_register_tool()}}, which builds a tool
from a bare function's formals, this passes each element through the chat's
tool normalizer, so pre-built \code{ToolDef}s keep their declared argument schema.
}
\examples{
\dontrun{
tools <- list(
  hal_tool(function(x, y) x + y, "add",
    description = "Add two numbers",
    types = list(x = "number", y = "number")
  ),
  hal_tool(function(path) readLines(path), "read_file",
    description = "Read a text file"
  )
)
hal_register_tools(tools)
}
}
\seealso{
\code{\link[=hal_register_tool]{hal_register_tool()}}, \code{\link[=hal_register_tool_specs]{hal_register_tool_specs()}}
}
