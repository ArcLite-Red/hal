% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quota.R
\name{hal_quota}
\alias{hal_quota}
\title{Claude 5-hour window quota status}
\usage{
hal_quota()
}
\value{
A \code{hal_quota} object (list) with fields \code{type}, \code{status},
\code{resets_at} (POSIXct), \code{overage_status}, \code{overage_disabled_reason},
\code{backend}. Returns \code{NULL} invisibly if no rate-limit info has been
observed, or if the active backend is Copilot.
}
\description{
Returns the most recent \code{rate_limit_event} payload observed on the Claude
backend during this session. Only populated after at least one \code{hal()},
\code{hal_ask()}, or \code{hal_do()} call -- the event is embedded in the prompt
response stream.
}
\examples{
\dontrun{
hal("hello")
hal_quota()
}

}
