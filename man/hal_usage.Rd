% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage.R
\name{hal_usage}
\alias{hal_usage}
\title{Session usage summary}
\usage{
hal_usage(tiers = NULL)
}
\arguments{
\item{tiers}{Optional named character vector mapping \code{modelId} to usage
tier (e.g. \code{c("claude-sonnet-5" = "1x")}). When \code{NULL} (default), uses
the cached tier map populated from the last \code{\link[=hal_models]{hal_models()}} call in this
session, or fetches it if none is cached and a live CLI is available.
Pass a pre-computed map to skip the network hop.}
}
\value{
A \code{hal_usage} data frame with columns \code{model}, \code{turns}, \code{tier},
\code{units} (numeric tier × turns, \code{NA} if tier unknown). Prints a compact
summary including the session total.
}
\description{
Tallies \code{hal()} turns by model and, when tier info is available, computes
cumulative Copilot billing units. Usage is tracked per session in memory
and cleared by \code{\link[=hal_reset]{hal_reset()}}.
}
\examples{
\dontrun{
hal("hello")
hal("follow-up", model = "gpt-4.1")
hal_usage()
}

}
