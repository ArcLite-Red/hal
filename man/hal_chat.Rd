% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hal-chat.R
\name{hal_chat}
\alias{hal_chat}
\title{Create a Copilot chat session}
\usage{
hal_chat(
  model = NULL,
  system_prompt = NULL,
  client = NULL,
  echo = NULL,
  on_text = NULL,
  on_tool_call = NULL,
  on_thought = NULL,
  mode = NULL,
  permission_policy = "auto-allow",
  quiet = FALSE
)
}
\arguments{
\item{model}{Model identifier (e.g., \code{"claude-sonnet-5"}, \code{"gpt-5.2"}).
Use \code{\link[=hal_models]{hal_models()}} to see available models. If \code{NULL}, falls back to
the \code{COPILOT_MODEL} environment variable, then the server default.}

\item{system_prompt}{System prompt string. Defaults to hal's built-in R
assistant prompt. Pass a string to override, or set via
\code{hal_configure(system_prompt = "...")}.}

\item{client}{An existing \link{HalClient} instance, or \code{NULL} to create
one automatically.}

\item{echo}{Echo mode: \code{"none"}, \code{"output"}, or \code{"all"}. Defaults to
\code{"output"} in interactive sessions, \code{"none"} otherwise.}

\item{on_text}{Callback for streaming text chunks: \verb{function(chunk)}.
Called for each \code{agent_message_chunk} as it arrives.}

\item{on_tool_call}{Callback for tool call events: \verb{function(tool_call)}.
Called with a \code{hal_tool_call} object on tool start and completion.}

\item{on_thought}{Callback for thought chunks: \verb{function(chunk)}.
Called for each \code{agent_thought_chunk} as it arrives.}

\item{mode}{Session mode: \code{"agent"} (default), \code{"plan"}, or \code{"autopilot"}.
Applied after the session is created on the first prompt.}

\item{permission_policy}{Permission policy for tool calls. One of
\code{"auto-allow"} (default), \code{"auto-deny"}, \code{"ask"} (interactive prompt;
denies in non-interactive sessions; vscode backend only), or a function
receiving \code{list(backend, tool_name, input)} and returning a string
containing \code{"allow"} or \code{"deny"}. The copilot backend additionally
accepts functions returning an ACP option id. Ignored if \code{client} is
provided.}

\item{quiet}{Logical; suppress informational messages (default \code{FALSE}).}
}
\value{
A \link{HalChat} object.
}
\description{
Creates a new \link{HalChat} instance for conversing with GitHub Copilot
models. This is the recommended entry point -- equivalent to
\code{HalChat$new()} but follows tidyverse naming conventions.
}
\examples{
\dontrun{
# Default model
chat <- hal_chat()
chat$chat("Explain the pipe operator in R")

# Specific model
chat <- hal_chat(model = "gpt-5.2")
chat$chat("Hello!")

# With system prompt
chat <- hal_chat(
  model = "claude-haiku-4.5",
  system_prompt = "Reply concisely in one sentence."
)
chat$chat("What is R?")

# Plan mode
chat <- hal_chat(mode = "plan")
chat$chat("Create an analysis plan for this dataset")

# With streaming callbacks
chat <- hal_chat(
  on_text = function(chunk) cat(chunk),
  on_tool_call = function(tc) message("Tool: ", tc$title)
)

# Read-only mode (deny file writes and shell commands)
chat <- hal_chat(permission_policy = "auto-deny")
chat$chat("What files are in R/?")
}
}
\seealso{
\code{\link[=hal_models]{hal_models()}}, \code{\link[=hal_client]{hal_client()}}, \code{\link[=hal_available]{hal_available()}}
}
