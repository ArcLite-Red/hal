% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find-cli.R
\name{hal_available}
\alias{hal_available}
\title{Check whether the active backend's transport is ready}
\usage{
hal_available(backend = NULL)
}
\arguments{
\item{backend}{Optional backend id (\code{"vscode"}, \code{"copilot"}, \code{"claude"}).
If \code{NULL}, uses the resolved default for this session.}
}
\value{
\code{TRUE} if the backend's transport is reachable, \code{FALSE} otherwise.
}
\description{
Dispatches by backend:
\itemize{
\item \code{vscode}: bridge port file exists and \verb{/version} responds.
\item \code{copilot}: Copilot CLI binary found and \code{--version} succeeds.
\item \code{claude}: Claude CLI binary found and \code{--version} succeeds.
}
}
\details{
Honors the \code{hal.backend} option / explicit argument so callers can probe
a specific backend without switching the session default.
}
\examples{
hal_available()
\dontrun{
hal_available(backend = "vscode")
hal_available(backend = "claude")
}

}
\seealso{
\link{HalChat}, \code{\link[=hal_models]{hal_models()}}, \code{\link[=hal_bridge_status]{hal_bridge_status()}} for richer
vscode-backend diagnostics.
}
