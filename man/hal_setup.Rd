% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{hal_setup}
\alias{hal_setup}
\title{Set up hal for your environment}
\usage{
hal_setup(force = FALSE, quiet = FALSE, backend = NULL)
}
\arguments{
\item{force}{Logical; re-run setup even if the backend is already ready.}

\item{quiet}{Logical; suppress progress messages.}

\item{backend}{Optional backend override (\code{"vscode"}, \code{"copilot"},
\code{"claude"}). When \code{NULL}, picks \code{vscode} in Positron and \code{copilot}
elsewhere.}
}
\value{
Invisibly \code{TRUE} if the backend transport is available after
setup, \code{FALSE} otherwise.
}
\description{
Interactive helper that picks the right backend for the host and walks
through prerequisites. Two paths:
}
\details{
\itemize{
\item \strong{Positron} (recommended): installs the hal-bridge extension from the
VSIX bundled with hal, so hal talks to \code{vscode.lm} directly. No
Node.js, no Copilot CLI, no GitHub CLI, no download -- the only
requirement is that you are signed in to GitHub Copilot inside
Positron itself (the account menu in the lower left).
\item \strong{Other hosts} (RStudio, VS Code, command-line R): installs the
GitHub Copilot CLI (\verb{gh copilot} extension preferred, npm
\verb{@github/copilot} fallback).
\item \strong{Claude Code}: verifies the \code{claude} CLI and points at the download
and one-time sign-in. hal does not install this one for you: Claude
Code's sign-in is an interactive browser flow, and installing it via
npm yields a shim whose output is unreliable when driven from R.
}

Detection is via \code{POSITRON_VERSION}. Pass \code{backend = "copilot"} to force
the Copilot CLI path inside Positron, or \code{backend = "claude"} to check the
Claude Code CLI instead.
}
\examples{
\dontrun{
hal_setup()                          # auto: vscode in Positron, copilot elsewhere
hal_setup(backend = "copilot")       # force the Copilot CLI path
hal_setup(backend = "claude")        # check the Claude Code CLI
}
}
\seealso{
\code{\link[=hal_available]{hal_available()}}, \code{\link[=hal_install_bridge]{hal_install_bridge()}},
\code{\link[=hal_bridge_status]{hal_bridge_status()}}, \code{\link[=hal_models]{hal_models()}}
}
