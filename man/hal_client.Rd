% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hal-client.R
\name{hal_client}
\alias{hal_client}
\title{Create a Copilot ACP client}
\usage{
hal_client(
  model = NULL,
  cli_path = NULL,
  permission_policy = "auto-allow",
  on_text = NULL,
  on_tool_call = NULL,
  on_thought = NULL,
  quiet = FALSE
)
}
\arguments{
\item{model}{Model identifier (e.g., \code{"claude-sonnet-5"}, \code{"gpt-5.2"}).
Passed as \code{--model} to the CLI at startup. If \code{NULL}, falls back to the
\code{COPILOT_MODEL} environment variable, then the server default.}

\item{cli_path}{Path to the Copilot CLI binary. If \code{NULL}, searches
\code{PATH} and common install locations.}

\item{permission_policy}{How to handle tool permission requests:
\code{"auto-allow"} (default) auto-approves, \code{"auto-deny"} auto-denies,
\code{"ask"} prompts interactively (vscode backend only), or a function
receiving permission params. The function returns an ACP option id
on copilot, or a string containing \code{"allow"}/\code{"deny"} on
vscode/claude.}

\item{on_text}{Callback for streaming text chunks: \verb{function(chunk)}.}

\item{on_tool_call}{Callback for tool call events: \verb{function(tool_call)}.}

\item{on_thought}{Callback for thought chunks: \verb{function(chunk)}.}

\item{quiet}{Logical; suppress informational messages (default \code{FALSE}).}
}
\value{
A \link{HalClient} object.
}
\description{
Creates a new \link{HalClient} instance for low-level ACP communication.
Most users should use \code{\link[=hal_chat]{hal_chat()}} instead.
}
\examples{
\dontrun{
client <- hal_client()
client$start()
client$handshake()
session <- client$new_session()
client$stop()
}
}
\seealso{
\code{\link[=hal_chat]{hal_chat()}}, \code{\link[=hal_available]{hal_available()}}
}
