% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-builder.R
\name{hal_register_tool}
\alias{hal_register_tool}
\title{Register an R Function as a Copilot Tool}
\usage{
hal_register_tool(fn, name = NULL, description = NULL, types = NULL)
}
\arguments{
\item{fn}{The R function to expose as a tool.}

\item{name}{Tool name (defaults to the symbol name of \code{fn}).}

\item{description}{Description shown to the LLM (when to call this tool).}

\item{types}{Optional named list mapping argument names to JSON Schema type
strings: \code{"string"}, \code{"number"}, \code{"integer"}, \code{"boolean"}, \code{"array"},
\code{"object"}.}
}
\value{
Invisibly returns the tool definition.
}
\description{
Shorthand that builds a tool definition and registers it on the active
session's chat object.
}
\examples{
\dontrun{
get_time <- function(tz = "UTC") format(Sys.time(), tz = tz)
hal_register_tool(get_time,
  description = "Current time in a given timezone"
)
hal("what time is it in Tokyo?")
}
}
