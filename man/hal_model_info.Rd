% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{hal_model_info}
\alias{hal_model_info}
\title{Inspect a single model in detail}
\usage{
hal_model_info(id = NULL, client = NULL)
}
\arguments{
\item{id}{Model identifier. If \code{NULL}, prints available ids and returns
them invisibly.}

\item{client}{A \link{HalClient} instance, or \code{NULL} to create one.}
}
\value{
A list with \code{id}, \code{name}, \code{description}, \code{provider}, \code{family},
\code{usage}, \code{multiplier}, \code{is_default}, \code{context_window}, \code{release_date},
and \code{meta} (raw \verb{_meta} list from the server, or \code{NULL} for Claude).
}
\description{
Returns the full per-model record, including the raw \verb{_meta} payload from
the ACP server. Useful for debugging or accessing fields that
\code{\link[=hal_models]{hal_models()}} doesn't surface as columns.
}
\examples{
\dontrun{
ids <- hal_model_info()   # print available ids
hal_model_info(ids[[1]])  # inspect the first one
}
}
