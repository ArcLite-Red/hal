% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{hal_models}
\alias{hal_models}
\title{List available models}
\usage{
hal_models(client = NULL)
}
\arguments{
\item{client}{A \link{HalClient} instance, or \code{NULL} to create one (Copilot only).}
}
\value{
A data frame with columns:
\itemize{
\item \code{id} — model identifier (matches \code{--model} flag)
\item \code{name} — display name
\item \code{description} — vendor-supplied prose (may be \code{NA})
\item \code{provider} — \code{"openai"}, \code{"anthropic"}, \code{"google"}, or \code{"unknown"}
\item \code{family} — coarse tier: \code{"light"}, \code{"standard"}, \code{"heavy"}
\item \code{usage} — billing tier label (e.g. \code{"1x"}, \code{"0.33x"}, \code{"0x"})
\item \code{multiplier} — numeric form of \code{usage}
\item \code{is_default} — \code{TRUE} if currently selected as session default
\item \code{context_window} — token limit when known (\code{NA} otherwise)
\item \code{release_date} — ISO date when known (\code{NA} otherwise)
}
}
\description{
Returns a data frame describing the models exposed by the active backend.
}
\details{
\itemize{
\item \strong{Copilot}: queries the ACP server (\code{session/new}) to fetch live
model metadata.
\item \strong{Claude}: returns the alias selectors (\code{opus}/\code{sonnet}/\code{haiku}), which
Anthropic resolves to the latest model of each tier -- the Claude CLI
exposes no live model list. Pass a concrete id (e.g. \code{"claude-opus-5"})
to \verb{model=} if you need to pin a version.
\item \strong{vscode}: queries the hal-bridge \verb{/models} endpoint, which returns
whatever models \code{vscode.lm} exposes to your Copilot session.
}
}
\examples{
\dontrun{
hal_models()
subset(hal_models(), provider == "anthropic")
}
}
\seealso{
\code{\link[=hal_model_info]{hal_model_info()}} for full per-model detail (incl. raw \verb{_meta}).
}
