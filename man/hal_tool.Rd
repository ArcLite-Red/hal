% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-builder.R
\name{hal_tool}
\alias{hal_tool}
\title{Create a Tool Definition from an R Function}
\usage{
hal_tool(fn, name = NULL, description = NULL, types = NULL)
}
\arguments{
\item{fn}{The R function to expose as a tool.}

\item{name}{Tool name (defaults to the symbol name of \code{fn}).}

\item{description}{Description shown to the LLM (when to call this tool).}

\item{types}{Optional named list mapping argument names to JSON Schema type
strings: \code{"string"}, \code{"number"}, \code{"integer"}, \code{"boolean"}, \code{"array"},
\code{"object"}.}
}
\value{
A list with \code{name}, \code{description}, \code{fun}, and \code{parameters} -- ready
for \code{chat$register_tool()}.
}
\description{
Converts an R function into a hal tool definition suitable for
\verb{$register_tool()}. Infers parameter types from function formals;
override with the \code{types} argument for non-string parameters.
}
\examples{
my_sum <- function(x, y) x + y
tool <- hal_tool(my_sum, "add_numbers",
  description = "Add two numbers",
  types = list(x = "number", y = "number")
)
str(tool)
\dontrun{
# register on the active session so the model can call it
hal_register_tools(list(tool))
}

}
