% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{hal_install_bridge}
\alias{hal_install_bridge}
\title{Install the hal-bridge Positron extension}
\usage{
hal_install_bridge(local_path = NULL, force = FALSE)
}
\arguments{
\item{local_path}{Optional path to a \code{.vsix} to install instead of the
bundled one. Useful for testing a dev build of the bridge.}

\item{force}{If \code{TRUE}, pass \code{--force} to the Positron CLI so an existing
install is replaced.}
}
\value{
Invisibly returns the installed VSIX path.
}
\description{
Installs the hal-bridge VSIX shipped with this hal release into Positron.
No download, no GitHub auth, no SHA verification: the bytes installed are
the bytes that shipped in \verb{inst/extdata/}. Reload Positron after
installing to activate the bridge.
}
\details{
The vscode backend (\code{hal_configure(backend = "vscode")}) requires this
extension. Other backends (\code{copilot}, \code{claude}) do not.
}
\examples{
\dontrun{
hal_install_bridge()
hal_install_bridge(local_path = "~/Downloads/hal-bridge-dev.vsix")
}
}
\seealso{
\code{\link[=hal_bridge_status]{hal_bridge_status()}} to check whether it's actually running.
}
