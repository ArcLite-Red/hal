% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/configure.R
\name{hal_configure}
\alias{hal_configure}
\title{Configure hal Options}
\usage{
hal_configure(
  use_colors = NULL,
  stream = NULL,
  stream_speed = NULL,
  default_model = NULL,
  system_prompt = NULL,
  eval_denylist = NULL,
  credential_action = NULL,
  eval_timeout = NULL,
  prompt_timeout = NULL,
  prompt_timeout_cold = NULL,
  edit_in_place = NULL,
  do_retries = NULL,
  do_on_fail = NULL,
  verify = NULL,
  plot_vision = NULL,
  permission_policy = NULL,
  session_quiet = NULL,
  init_quiet = NULL,
  show_thoughts = NULL,
  backend = NULL,
  quiet = FALSE
)
}
\arguments{
\item{use_colors}{Logical; enable ANSI color output (default: TRUE).}

\item{stream}{Logical; enable streaming typewriter output (default: TRUE).}

\item{stream_speed}{Character; \code{"instant"}, \code{"fast"}, \code{"medium"}, or
\code{"slow"} (default: \code{"medium"}).}

\item{default_model}{Character; default model identifier.}

\item{system_prompt}{Character; custom system prompt. Use \code{NULL} to keep
the current prompt. The placeholder \code{{user}} is replaced with the
current login name.}

\item{eval_denylist}{Character vector of function names blocked from
\code{eval_r} execution. Set to \code{FALSE} to disable. Default: curated list.}

\item{credential_action}{Character; action on credential detection:
\code{"warn"} (default), \code{"redact"}, or \code{"block"}.}

\item{eval_timeout}{Numeric; seconds for eval_r time limit (default: 30).}

\item{prompt_timeout}{Numeric; seconds before aborting a resumed prompt
on the Claude backend (default: 60). Has no effect on Copilot.}

\item{prompt_timeout_cold}{Numeric; seconds before aborting the first
(session-creating) prompt on the Claude backend (default: 180). The
cold path pays for Node.js startup, AV scanning, and session
provisioning on Windows and can exceed the warm timeout.}

\item{edit_in_place}{Logical; when TRUE, \code{hal_do()} (and \code{hal_excel()})
replaces itself in the editor with generated code. When unset, defaults
to TRUE where applicable -- i.e. when called from a saved IDE source
editor buffer -- and FALSE from the console. Set explicitly to force
either behaviour.}

\item{do_retries}{Integer; max retry attempts for \code{hal_do()} when generated
code fails to parse or execute (default: 2). Set to 0 to disable.}

\item{do_on_fail}{Character; what \code{hal_do()} does when generation fails
after all retries: \code{"warn"} warns and passes \code{.data} through unchanged;
\code{"abort"} raises an error. When unset, defaults to \code{"warn"} in
interactive sessions and \code{"abort"} in non-interactive contexts
(scripts, R Markdown, \code{targets}), where silently continuing with
untransformed data is worse than failing.}

\item{verify}{Logical; when TRUE (default), \code{hal_do()} pipe mode compares
input and output data frames and displays a one-line structural report,
attaching the full report as \code{attr(result, "hal_verify")}. Report-only:
never affects retries or values.}

\item{plot_vision}{Logical; when TRUE (default), plots drawn by \code{eval_r}
are captured as PNG and returned to the model as images so it can see
and iterate on them (vscode backend with hal-bridge >= 0.1.4, and
claude backend). The copilot backend is always text-only regardless of
this setting (its CLI's image forwarding is unverified). Notes: plots
written to file devices the code opens itself (\code{png()}, \code{pdf()}) are
not echoed; one image per eval (the final page); worst-case eval_r
time is ~2x \code{eval_timeout} (eval + render).}

\item{permission_policy}{Character or function; how to handle tool
permission requests. \code{"auto-allow"} (default) auto-approves;
\code{"auto-deny"} blocks every tool call; \code{"ask"} prompts interactively on
the vscode backend (denies in non-interactive sessions); a function
receives \code{list(backend, tool_name, input)} (copilot adds the full ACP
params) and returns a decision -- a string containing \code{"allow"} or
\code{"deny"} (vscode/claude), or an ACP option id (copilot). Takes effect
on next session init (call \code{hal_reset()} to apply immediately).}

\item{session_quiet}{Logical; suppress informational messages from the
transport layer (CLI startup, handshake). Takes effect on next session
init (call \code{hal_reset()} to apply immediately).}

\item{init_quiet}{Logical; when TRUE, suppress the data transmission notice
shown on first session init (default: FALSE).}

\item{show_thoughts}{Logical; when TRUE, display the model's reasoning/thought
process before the response (default: FALSE). Takes effect on next session
init (call \code{hal_reset()} to apply immediately).}

\item{backend}{Character; transport backend: \code{"vscode"} (default in
Positron) talks to the hal-bridge extension over localhost HTTP fronting
\code{vscode.lm}; \code{"copilot"} (default elsewhere) uses the GitHub Copilot CLI in
ACP mode; \code{"claude"} uses Anthropic's Claude Code CLI via \code{claude -p} with
session resume. Takes effect on next session init (call \code{hal_reset()} to
apply immediately).}

\item{quiet}{Logical; suppress confirmation messages (default: FALSE).}
}
\value{
Invisibly returns a list of changes made.
}
\description{
Set display, governance, and session defaults. Settings persist for the
duration of the R session via \code{options()}.
}
\examples{
hal_configure(stream = FALSE, do_retries = 1, quiet = TRUE)
hal_config()$stream
# unset again to restore the defaults
options(hal.stream = NULL, hal.do_retries = NULL)
}
