% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hal-excel.R
\name{hal_excel}
\alias{hal_excel}
\alias{print.hal_excel_code}
\title{Turn an Excel sheet into R: code that reads the data and recreates the formulas}
\usage{
hal_excel(path, sheet = 1, model = NULL, tolerance = NULL, retries = NULL)

\method{print}{hal_excel_code}(x, ...)
}
\arguments{
\item{path}{Path to an \code{.xlsx} workbook.}

\item{sheet}{Sheet name, or 1-based index (default \code{1}).}

\item{model}{Optional model id; defaults to the session/configured model.}

\item{tolerance}{Numeric tolerance for float comparison (default
\code{hal.xl_tolerance}, or \code{1e-9}).}

\item{retries}{Max self-correction attempts per column on mismatch (default
\code{hal.xl_retries}, then \code{hal.do_retries}, then \code{2}).}

\item{x}{A \code{hal_excel_code} object (the generated R script).}

\item{...}{Ignored.}
}
\value{
A \code{hal_excel_code} object (a character string of R code) that prints as
the generated script. The per-column report is attached as
\code{attr(result, "hal_excel")}. Save it with \code{writeLines(result, "out.R")}.
}
\description{
Reads an \code{.xlsx}, treats columns without formulas as data, and translates
each formula column into a tidyverse expression via the active hal backend.
Returns a runnable R script -- a line that reads the input columns from the
workbook plus a \code{mutate()} pipeline that recreates the formula columns -- so
you can replace the spreadsheet (and the \code{hal_excel()} call) with the code.
}
\details{
Works like \code{\link[=hal_do]{hal_do()}} under the hood (disposable worker, code extraction,
denylist, timed eval, retry loop), with one addition: each translation is run
and checked against the values Excel cached, so only columns that match Excel
row-for-row go into the live pipeline; the rest are emitted as commented
stubs to review.

Each formula column is assumed to hold one formula filled down the column
(a vectorised expression, e.g. \verb{=A2*B2}), so its first formula is translated
once. A workbook with no cached values is still translated but reported
\code{unverifiable}.
}
\examples{
\dontrun{
hal_excel("model.xlsx")                # prints the read + mutate script
code <- hal_excel("model.xlsx")
writeLines(code, "model.R")         # or paste it in place of the call
attr(code, "hal_excel")               # per-column verification report
}

}
