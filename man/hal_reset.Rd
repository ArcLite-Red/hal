% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{hal_reset}
\alias{hal_reset}
\title{Reset the hal session}
\usage{
hal_reset(mode = NULL)
}
\arguments{
\item{mode}{Session mode: \code{"agent"} (default), \code{"plan"}, or \code{"autopilot"}.
If NULL, uses the default (agent).}
}
\value{
Invisibly returns \code{NULL}, called for its side effect.
}
\description{
Destroys the current session and starts fresh.
}
\examples{
\dontrun{
hal_reset()               # fresh session on the same backend
hal_reset(mode = "plan")  # restart in plan mode (copilot backend)
}
}
