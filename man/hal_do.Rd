% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe-verbs.R
\name{hal_do}
\alias{hal_do}
\title{Code Generation via LLM -- Pipe and Standalone Modes}
\usage{
hal_do(.data, ..., .model = NULL, .retries = NULL, .verify = NULL)
}
\arguments{
\item{.data}{In pipe mode: an R object piped in. In standalone mode: the
first prompt string.}

\item{...}{Character strings describing the transformation/task.}

\item{.model}{Model to use for code generation (default: session model).}

\item{.retries}{Integer; max retry attempts when generated code fails to
parse or execute (default: 2, configurable via \code{hal.do_retries} option).
Set to 0 to disable retries. The retry re-prompts the same disposable
session with the error message, so the model can self-correct.}

\item{.verify}{Logical; in pipe mode with a data-frame result, compare the
input and output and display a one-line structural report (row/column
deltas, class changes, introduced NAs). The full report is attached as
\code{attr(result, "hal_verify")}. Report-only: it never affects retries or
the returned values, but suspicious patterns (output identical to
input, 0-row output) raise a classed \code{hal_do_warning}. Default TRUE
(configurable via the \code{hal.verify} option).}
}
\value{
\strong{Pipe mode:} transformed data. \strong{Standalone:} result of generated
code (invisible).
}
\description{
\strong{Pipe mode}: a mid-pipe transformation verb that generates R code,
executes it, and returns the result.
}
\details{
\strong{Standalone mode}: called directly with a string prompt to generate
and execute R code in the caller's environment.

Spawns a disposable session -- does NOT pollute the main conversation.
Governance controls (denylist, credential scanner, timeout) are enforced.
}
\section{Failure behavior}{

In an \strong{interactive} session, a failure (after retries) warns and returns
\code{.data} unchanged (pipe) or \code{invisible(NULL)} (standalone) -- forgiving
for console exploration. In \strong{non-interactive} contexts (scripts, R
Markdown, \code{targets}), it aborts instead: a pipeline silently continuing
with untransformed data is worse than an error. Override either way with
\code{options(hal.do_on_fail = "warn")} or \code{"abort"}. All failures signal
classed conditions (\code{hal_do_error} / \code{hal_do_warning}), so you can
\code{tryCatch(..., hal_do_error = function(e) ...)}.
}

\examples{
\dontrun{
# Pipe mode
mtcars |> hal_do("keep only cars with mpg > 25")
iris |> hal_do("add a column for petal area") |> head()

# Standalone mode
hal_do("write a function called greet that takes a name")
greet("world")

# Disable retries
mtcars |> hal_do("normalize mpg", .retries = 0)
}

}
