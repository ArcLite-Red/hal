% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe-verbs.R
\name{hal_ask}
\alias{hal_ask}
\title{Ask Copilot a Question -- Pipe and Standalone Modes}
\usage{
hal_ask(.data, ...)
}
\arguments{
\item{.data}{In pipe mode: an R object (data frame, model, etc.) piped in.
In standalone mode: the first prompt string.}

\item{...}{Character strings forming the prompt.}
}
\value{
\strong{Pipe mode:} invisibly returns \code{.data} (pipe passthrough).
\strong{Standalone:} invisibly returns the response text.
}
\description{
\strong{Pipe mode}: a pipe-terminal verb that sends a prompt to Copilot augmented
with the piped data context. The piped object is described via \code{str()} and
attached to the prompt. Returns \code{.data} invisibly (pipe passthrough).
}
\details{
\strong{Standalone mode}: called directly with a string prompt to ask a question
without data context. Returns the response text invisibly.

Spawns a disposable session -- does NOT pollute the main \code{hal()} conversation.
}
\examples{
\dontrun{
# Pipe mode (data-aware analysis)
mtcars |> hal_ask("what are the key patterns?")
lm(mpg ~ wt, data = mtcars) |> hal_ask("interpret these coefficients")

# Standalone mode (general question)
hal_ask("what does the pipe operator do in R?")
hal_ask("explain", "the difference between <- and =")
}

}
